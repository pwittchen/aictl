<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<key>com.apple.security.device.audio-input</key>
	<true/>
	<!--
		`keychain-access-groups` puts the desktop, CLI and `aictl-server`
		in the same Keychain partition so locking/unlocking keys in one
		binary does not trigger per-binary password prompts in the
		others. The CI signing step substitutes `__TEAM_ID__` with the
		value of the `APPLE_TEAM_ID` repo secret before passing this
		file to `codesign --entitlements`. The matching declarations
		live in `crates/aictl-cli/entitlements.plist` and
		`crates/aictl-server/entitlements.plist`.
	-->
	<key>keychain-access-groups</key>
	<array>
		<string>__TEAM_ID__.com.piotrwittchen.aictl</string>
	</array>
</dict>
</plist>
