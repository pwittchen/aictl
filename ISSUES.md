## Issues

- add `web_search` tool with firecrawl API
- remove possibility to provide api key via terminal
- add interactive mode when user enters program name without params
- in the interactive mode, add nice terminal UI
- add tech info like: tokens for each api call, used tools during the session, number of iterations, size of the context in percent
- add context limit and compacting function
- add flow and arch diagram
